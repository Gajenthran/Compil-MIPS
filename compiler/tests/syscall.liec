/*
 * Vérification des appels systèmes à savoir
 * l'affichage d'un nombre ou d'une chaîne de
 * caractère
 */
int main(argc, argv : int, str list) {
  print_num(12);
  // print_str("nl");
  return 0;
}