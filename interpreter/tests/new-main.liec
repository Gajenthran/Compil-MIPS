/* 
 * Nombre à virgule
 * Alternative (avec option)
 * Argument d'une fonction
 * Return fonction 
 * boucle while/for
 * portée des variables
 * bloc d'une fonction/structure de contrôle
 */


str list lste = ["toto" , "popo"];
int nb = 4;

int square(n : int) {
  n * n
};

int setNb(n : int) {
  nb = n;
  1
};

int max(m, n : int, int) {
  if(m > n) then
    m
  else
    n
};

rec int fact(n, res: int, int) {
    if(n <= 1) then {
      print_str("récursion finie !\n");
      res * n
    }

    else {
      print_lst(lste);
      fact(n - 1, res * n);
      res * n
    }
};

int main(argc, argv : int, str list) {
  nb = 10;
  /* while(nb < 5) then {
    nb = nb + 1;
    print_num nb;
    1
  }; */

  if(nb <= 0) then {
    print_str("négatif");
    nb = 100;
    1
  }

  else {
    print_str("positif");
    nb = 0;
    1
  };

  int toto = 2;
  bool b = false;

  nb = max(toto, nb);
  nb = fact(3, 1);
  // setNb(100);
  print_num(atoi(strcat("98", "38")) / 2);
  print_num(nb);
  1294
}