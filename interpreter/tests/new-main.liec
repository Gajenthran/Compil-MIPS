str list lste = {"toto" , "popo"};
int nb = 4 + 2 + 3;

int square(n : int) {
  print_str("toto");
  return n * n;
}

int setNb(n : int) {
  nb = n;
  return 1;
}

rec int fact(n, res: int, int) {
  print_int(n);
    if(n <= 1) {
      print_str("récursion finie !\n");
    }

    else {
      // print_lst(lste);
      fact(n - 1, res * n);
  }
  return res;
}

int main(argc, argv : int, str list) {
  int a = 10;
  print_int(12);
  return 0;
}